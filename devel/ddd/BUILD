(
  CFLAGS="${CFLAGS} -fpermissive"      &&
  CXXFLAGS="${CXXFLAGS} -fpermissive"  &&
  default_build
#  ./configure --prefix=${INSTALL_ROOT}/usr $OPTS &&
#  make                   &&
#  prepare_install        &&
#  make    install        

) > $C_FIFO 2>&1
