Haven't fully tested this yet, so waiting to pull into test.

At least module-init-tools 3.0-pre5 fails with this, so we can't pull it in yet.

According to Jason, module-init-tools, unixODBC and xfree86 fail with this.

