sedit  's:$(prefix)/info:$(prefix)/share/info:'  Makefile.in  &&

./configure  --build=$BUILD                \
             --prefix=${INSTALL_ROOT}/usr  \
             $OPTS                         &&
make
