default_pre_build
cd      $SOURCE_DIRECTORY
unpack  $SOURCE2  ${MD5[1]}

for i in 3 5 6 7 8 9
do
        SVAR=SOURCE$i
        message "${MESSAGE_COLOR} Applying patch ${!SVAR} ${DEFAULT_COLOR}"
        unpack ${!SVAR} ${MD5[$(($i-1))]}
        patch -p0 < $SOURCE_CACHE/${!SVAR}
done
