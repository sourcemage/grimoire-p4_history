default_pre_build                             &&
cd      $SOURCE_DIRECTORY                     &&
mkdir -p Completion/SourceMage/Command        &&
bzcat   $SCRIPT_DIRECTORY/functions.tar.bz2|  \
tar xf - -C Completion/SourceMage/Command     &&
patch -p1 < $SCRIPT_DIRECTORY/add_sourcemage_dir.patch
