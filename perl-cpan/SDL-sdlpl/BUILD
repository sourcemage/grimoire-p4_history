(

  perl  Makefile.PL INC="-I/usr/include -I/usr/include/SDL"  &&
  make                                                       &&
  prepare_install                                            &&
  make    install

) > $C_FIFO 2>&1
