(

  sedit 's/Perl_sv_isa/sv_isa/' Curses.c &&
  perl  Makefile.PL                      &&
  make                                   &&
  prepare_install                        &&
  make    install

) > $C_FIFO 2>&1
