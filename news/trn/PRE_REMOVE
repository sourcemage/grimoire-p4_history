if [ -d /usr/lib/news/active ]; then
  if query "$SPELL used /usr/lib/news/active directory, do you want to remove it ?" y
  then
    rm -fr /usr/lib/news/active 1>/dev/null 2>&1
  else
    true
  fi
fi
