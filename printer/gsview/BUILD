(

  cp  srcunx/unx.mak  Makefile                    &&
  make                                            &&
  prepare_install                                 &&
  make  GSVIEW_ROOT=${INSTALL_ROOT}/usr  install

) > $C_FIFO 2>&1
