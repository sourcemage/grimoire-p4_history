(

  prepare_install                                                      &&
  mkdir  -p                     ${INSTALL_ROOT}/usr/share/ghostscript  &&
  cp     -r  $SOURCE_DIRECTORY  ${INSTALL_ROOT}/usr/share/ghostscript

) > $C_FIFO 2>&1
