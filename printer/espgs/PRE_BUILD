if  [  "$SOURCE2"  !=  ""  ];
then
  default_pre_build          &&
  cd      $SOURCE_DIRECTORY  &&
  unpack  $SOURCE2
else
  default_pre_build
fi

rm  -f  ${SOURCE_DIRECTORY}/Makefile.in  &&
cp  ${SOURCE_DIRECTORY}/src/Makefile.in  \
    ${SOURCE_DIRECTORY}/Makefile.in
