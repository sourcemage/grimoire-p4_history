(

  prepare_install                         &&
  cp     -r   usr/*            /usr       &&
  mkdir  -p  /etc/cups                    &&
  cp     -r   var/cups/conf/*  /etc/cups  &&
  cp     -r   var/*            /var

) > $C_FIFO 2>&1
