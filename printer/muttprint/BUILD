(

  sedit  's:/usr/local:/usr:'                   Makefile  &&
  sedit  's:/usr/share/doc/packages:/usr/doc:'  Makefile  &&
  sedit  's:/share/man:/man:'                   Makefile  &&

  prepare_install                                         &&
  make    install

) > $C_FIFO 2>&1
