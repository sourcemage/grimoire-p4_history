if  !  spell_installed  ghostscript  &&  !  spell_installed  espgs;  then
  message  "\n${PROBLEM_COLOR}  **  Do NOT choose ghostscript if you have installed or are installing cups:  **  ${DEFAULT_COLOR}\n"
fi  &&

depends  POSTSCRIPT-INTERPRETER
