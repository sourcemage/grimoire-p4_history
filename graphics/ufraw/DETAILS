           SPELL=ufraw
if [ "$UFRAW_CVS" == "y" ]; then
  if [ "$UFRAW_CVS_AUTOUPDATE" == "y" ]; then
         VERSION=$(date +%Y%m%d)
  else
         VERSION=cvs
  fi
          SOURCE=$SPELL-cvs.tar.bz2
   SOURCE_URL[0]=cvs://:pserver:anonymous@cvs.sourceforge.net:/cvsroot/ufraw:ufraw
  FORCE_DOWNLOAD=on
          MD5[0]="IGNORE"
SOURCE_DIRECTORY=$BUILD_DIRECTORY/$SPELL-cvs
else
         VERSION=0.4
          SOURCE=$SPELL-$VERSION.tar.gz
   SOURCE_URL[0]=$SOURCEFORGE_URL/$SPELL/$SOURCE
          MD5[0]='a496dcd447c98bd2f51aca2b9328f670'
SOURCE_DIRECTORY=$BUILD_DIRECTORY/$SPELL-$VERSION
fi
        WEB_SITE=http://ufraw.sourceforge.net/
         ENTERED=20041112
         UPDATED=20041112
      LICENSE[0]=GPL
       BUILD_API=2
           SHORT="A Gimp plug-in which allow loading images in raw format."
cat << EOF
A Gimp plug-in which allow loading images in raw format.
EOF
