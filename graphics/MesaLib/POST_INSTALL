# Install SGI's GLU 1.3 implementation (Mesa contains 1.1/1.2 only)
cd  /usr/X11R6/lib/            &&
unpack  ${SOURCE2}  ${MD5[1]}  &&
cd  /usr/lib                   &&
unpack  ${SOURCE2}  ${MD5[1]}
