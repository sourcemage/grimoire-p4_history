(

  ./configure  --prefix=/usr             \
               --sysconfdir=/etc/imlib2  &&
  make                                   &&
  prepare_install                        &&
  make    install

) > $C_FIFO 2>&1
