(

  ./configure  --prefix=/usr    \
               --enable-shared  \
               --enable-static  &&
  make                          &&
  prepare_install               &&
  make    install

) > $C_FIFO 2>&1
