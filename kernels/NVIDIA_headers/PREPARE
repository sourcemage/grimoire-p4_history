#simple hack to remove old cache before it becomes a problem version will need to be updated as 
#PREPARE doesn't know version and current version might not be cached. 
OLDCACHE="$INSTALL_CACHE/$SPELL-*-$BUILD.tar.bz2"
if [ -f $OLDCACHE ] ;
then rm -rf $OLDCACHE
fi &&
message "${SPELL_COLOR} ${SPELL} won't ARCHIVE installed files${DEFAULT_COLOR}"
