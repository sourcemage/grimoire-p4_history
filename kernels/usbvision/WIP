It appears that the tarball isn't being extracted, as I get complaints that mkdir can't create /usr/src/usbvision-0.9.8.2/src
If I extract the tarball by hand before casting, it builds without any error. Looks like a error in default_pre_build?
