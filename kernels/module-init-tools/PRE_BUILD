  mk_source_dir        $SOURCE_DIRECTORY   &&
  unpack_file                              &&

  if [ $OLD_TOO == y ]; then
    mk_source_dir        $SOURCE_DIRECTORY2  &&
    unpack               $SOURCE2 ${MD5[1]}
  fi

