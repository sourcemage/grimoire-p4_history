[ ! -d /var/log/trace ] && mkdir -p /var/log/trace/
[ ! -d /relay ] && mkdir /relay
if [ "`grep relayfs /etc/fstab`" == "" ]
then
	echo '#none	/relay	relayfs	defaults	0 0' >> /etc/fstab
fi
if [ ! -f /etc/sysconfig/tracedaemon ]
then
	cp ${SCRIPT_DIRECTORY}/tracedaemon /etc/sysconfig/
fi
