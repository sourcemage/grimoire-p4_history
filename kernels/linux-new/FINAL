# Add a lilo/grub/whatever entry for this kernel
export VERSION
$SCRIPT_DIRECTORY/UPDATE_BOOTLOADER 

#if [[ "$TREE" == 2.6 ]] && [[ "$(gaze installed module-init-tools | fgrep 'not installed')" != "" ]] ; then cast module-init-tools ; fi
# doesn't like the above line...

cp  ${SPELL_CONFIG}.p ${LINUX_SOURCE_DIRECTORY}/.spell-config.p

if [[ "$ARCHIVE" == "on" ]]
then
	message "${MESSAGE_COLOR}Copying everything from ${LINUX_SOURCE_DIRECTORY} to ${SOURCE_DIRECTORY} for archiving${DEFAULT_COLOR}"
	mk_source_dir ${SOURCE_DIRECTORY} &&
	cp -a ${LINUX_SOURCE_DIRECTORY}/* ${SOURCE_DIRECTORY}/
fi
