default_build  &&
(

  # install the logo pixmap, the makefile doesn't do this ...
  mkdir  -p                    ${INSTALL_ROOT}/usr/share/divxcalc/pixmaps  &&
  cp     pixmaps/divxcalc.xpm  ${INSTALL_ROOT}/usr/share/divxcalc/pixmaps

) > $C_FIFO 2>&1
