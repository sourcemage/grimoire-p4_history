default_build  &&
(

  # install the logo pixmap, the makefile doesn't do this ...
  mkdir  -p                    /usr/share/divxcalc/pixmaps  &&
  cp     pixmaps/divxcalc.xpm  /usr/share/divxcalc/pixmaps

) > $C_FIFO 2>&1
