if test "$(get_spell_provider $SPELL GECKO)" = "firefox"; then
  export DEST=$INSTALL_ROOT/usr/lib/firefox/plugins
else
  export DEST=$INSTALL_ROOT/usr/lib/mozilla/plugins
fi  &&
make install  &&
unset DEST
