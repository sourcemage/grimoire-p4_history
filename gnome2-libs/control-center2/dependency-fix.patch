--- configure~	2004-08-30 01:56:22.000000000 +0200
+++ configure	2004-09-06 13:45:49.000000000 +0200
@@ -25796,58 +25796,6 @@
 
   fi
 
-  if test "$PKG_CONFIG" = "no" ; then
-     echo "*** The pkg-config script could not be found. Make sure it is"
-     echo "*** in your path, or set the PKG_CONFIG environment variable"
-     echo "*** to the full path to pkg-config."
-     echo "*** Or see http://www.freedesktop.org/software/pkgconfig to get pkg-config."
-  else
-     PKG_CONFIG_MIN_VERSION=0.9.0
-     if $PKG_CONFIG --atleast-pkgconfig-version $PKG_CONFIG_MIN_VERSION; then
-        echo "$as_me:$LINENO: checking for libnautilus" >&5
-echo $ECHO_N "checking for libnautilus... $ECHO_C" >&6
-
-        if $PKG_CONFIG --exists "libnautilus" ; then
-            echo "$as_me:$LINENO: result: yes" >&5
-echo "${ECHO_T}yes" >&6
-            succeeded=yes
-
-            echo "$as_me:$LINENO: checking NAUTILUS_CFLAGS" >&5
-echo $ECHO_N "checking NAUTILUS_CFLAGS... $ECHO_C" >&6
-            NAUTILUS_CFLAGS=`$PKG_CONFIG --cflags "libnautilus"`
-            echo "$as_me:$LINENO: result: $NAUTILUS_CFLAGS" >&5
-echo "${ECHO_T}$NAUTILUS_CFLAGS" >&6
-
-            echo "$as_me:$LINENO: checking NAUTILUS_LIBS" >&5
-echo $ECHO_N "checking NAUTILUS_LIBS... $ECHO_C" >&6
-            NAUTILUS_LIBS=`$PKG_CONFIG --libs "libnautilus"`
-            echo "$as_me:$LINENO: result: $NAUTILUS_LIBS" >&5
-echo "${ECHO_T}$NAUTILUS_LIBS" >&6
-        else
-            NAUTILUS_CFLAGS=""
-            NAUTILUS_LIBS=""
-            ## If we have a custom action on failure, don't print errors, but
-            ## do set a variable so people can do so.
-            NAUTILUS_PKG_ERRORS=`$PKG_CONFIG --errors-to-stdout --print-errors "libnautilus"`
-            echo $NAUTILUS_PKG_ERRORS
-        fi
-
-
-
-     else
-        echo "*** Your version of pkg-config is too old. You need version $PKG_CONFIG_MIN_VERSION or newer."
-        echo "*** See http://www.freedesktop.org/software/pkgconfig"
-     fi
-  fi
-
-  if test $succeeded = yes; then
-     :
-  else
-     { { echo "$as_me:$LINENO: error: Library requirements (libnautilus) not met; consider adjusting the PKG_CONFIG_PATH environment variable if your libraries are in a nonstandard prefix so pkg-config can find them." >&5
-echo "$as_me: error: Library requirements (libnautilus) not met; consider adjusting the PKG_CONFIG_PATH environment variable if your libraries are in a nonstandard prefix so pkg-config can find them." >&2;}
-   { (exit 1); exit 1; }; }
-  fi
-
 
 
 
