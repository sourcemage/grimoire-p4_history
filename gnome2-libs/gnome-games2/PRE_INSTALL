if  [  "$HISCORES"  ==  "y"  &&  -f  $TRACK_ROOT/var/games/*.scores  ];  then
  message  "${YELLOW}${BOLD}Saving score files${DEFAULT}"  &&
  mkdir  -p  /tmp/${SPELL}.bkup                            &&
  cp  $TRACK_ROOT/var/games/*.scores  /tmp/${SPELL}.bkup
fi  &&

default_pre_install
