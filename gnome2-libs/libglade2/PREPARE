if spell_installed Python 
then
   if ! grep -q  "Python:expat:on:optional:" $DEPENDS_STATUS
   then
      message ${PROBLEM_COLOR} Python is not cast with expat enabled. Please recast Python and enable the expat optional depends. ${DEFAULT_COLOR}
      return 1
   fi
else
   message ${QUERY_COLOR} Please enable expat during the Python configure or casting will hang. ${DEFAULT_COLOR} &&
   sleep 5 
fi  
