if spell_installed Python && spell_installed tk &&
spell_installed tcl; then

   LIBPYTHON="-lpython`gaze installed Python | cut -c -3`"
   LIBPYTHON="$LIBPYTHON -ltcl`gaze installed tcl | cut -c -3`"
   LIBPYTHON="$LIBPYTHON -ltk`gaze installed tk | cut -c -3`"

   export LIBPYTHON
fi  &&

default_pre_build
