if [ `gaze installed | grep Python` -a `gaze installed | grep tcl-tk` ]
then
   pythonVer=`gaze installed Python`
   tclVer=`gaze installed tcl-tk | cut -c -3`

   export LIBPYTHON="-lpython$pythonVer -ltk$tclVer -ltcl$tclVer"
fi	&&

default_pre_build