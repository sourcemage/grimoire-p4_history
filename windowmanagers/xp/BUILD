(
  prepare_install                                                   &&
  mkdir -p ${INSTALL_ROOT}/usr/share/icewm/themes/                    &&
  cp  -r  $BUILD_DIRECTORY/$SPELL/  ${INSTALL_ROOT}/usr/share/icewm/themes/

) > $C_FIFO 2>&1
