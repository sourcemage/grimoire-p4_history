(

  make                              &&
  prepare_install                   &&
  make  INSTALLDIR=/usr/bin         \
        INCLUDEDIR=/usr/include     \
        MANDIR=/usr/share/man/man1  \
	LOCALEDIR=/usr/share/locale \
        install

) > $C_FIFO 2>&1
