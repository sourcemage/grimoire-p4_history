mk_source_dir $SOURCE_DIRECTORY &&
unpack $SOURCE ${MD5[0]} &&

# Galeon patch
cd $SOURCE_DIRECTORY &&
patch -p0 < $SCRIPT_DIRECTORY/gtk2mozembed.patch &&

# Enigmail extensions
cd $SOURCE_DIRECTORY/extensions/ &&
unpack $SOURCE2 ${MD5[1]} &&
unpack $SOURCE3 ${MD5[2]}
