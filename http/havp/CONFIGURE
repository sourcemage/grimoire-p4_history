config_query_string HAVP_PORT 'Port number to listen on?' 8080
config_query_list HAVP_INADDR \
                  'IP address to listen on [future use]?' \
                  '0.0.0.0' '127.0.0.1'
config_query_string HAVP_TMP \
                    'Hardlock-enabled partition to use for temporary storage?' \
                    '/var/spool/havp'
