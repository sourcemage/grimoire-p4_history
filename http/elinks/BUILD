if [ "$ELINKS_CVS" = 'y' ]; then                                                    
  ./autogen.sh
fi &&

OPTS="$OPTS
  --enable-exmode
  --enable-fastmem
  --enable-html-highlight
  --enable-leds
  --enable-nntp
  --without-gnutls
" &&

default_build
