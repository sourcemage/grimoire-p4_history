optional_depends  "zlib"       "--with-zlib"   "--without-zlib"   "for zlib compression"  &&
optional_depends  "mysql"      "--with-mysql"  "--without-mysql"  "for mysql access"      &&
optional_depends  "openssl"    "--with-ssl"    "--without-ssl"    "for SSL support"       &&
optional_depends  "coreutils"  "--with-md5"    "--without-md5"    "for MD5 support"       &&
optional_depends  "xfree86"    "--with-x"      "--without-x"      "for XFree86 support"

# expat breaks liboop
#optional_depends  "expat"      "--with-expat"  "--without-expat"  "for expat XML parsing support"
