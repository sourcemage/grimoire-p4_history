default_pre_build                             &&
cd $SOURCE_DIRECTORY                          &&
touch install.sh                              &&

mk_source_dir  $SOURCE_DIRECTORY.bld          &&

cd $SOURCE_DIRECTORY.bld                      &&

# reconstruct previous congfigure environment
unpack $SOURCE2                               &&

cp  -a save/* $SOURCE_DIRECTORY               &&
rm -r save
