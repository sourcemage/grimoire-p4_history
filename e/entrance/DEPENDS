depends  edb     &&
depends  edje    &&
depends  esmart  &&

if  [  "$ENTRANCE_PAM"  ==  "y"  ];  then
  depends  linux-pam  '--with-auth-mode=pam'
else
  depends  shadow  '--with-auth-mode=shadow'
fi
