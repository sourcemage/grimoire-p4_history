# optional depends for ANTLR
if  !  grep  -q  "WITH_ANTLR="  $SPELL_CONFIG;  then

  if    query  "Build ant with ANTLR support ? [optional]" n
    then  echo   "WITH_ANTLR=y"  >>  $SPELL_CONFIG ; WITH_ANTLR=y
    else  echo   "WITH_ANTLR=n"  >>  $SPELL_CONFIG
  fi
  
fi
