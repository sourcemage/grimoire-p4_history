unpack ${SOURCE[0]} ${MD5[0]} &&
cd $SOURCE_DIRECTORY &&
sedit '/^SRCDIR =/s|=.*$|= /usr/lib/xfractint|' Makefile &&
cp pars/* . &&
cp formulas/* . &&
cp lsystem/* . &&
sedit '267,269 d' Makefile #to remove line 267-269
