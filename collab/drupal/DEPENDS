message  "${MESSAGE_COLOR}${SPELL_COLOR}apache2${MESSAGE_COLOR} is"    \
         " recommended${DEFAULT_COLOR}"                                &&
depends  PHP                                                           &&

message  "${MESSAGE_COLOR}${SPELL_COLOR}mysql${DEFAULT_COLOR}"         \
         "${MESSAGE_COLOR} is recommended as this spell is setup for"  \
         "it${DEFAULT_COLOR}"                                          &&
depends  SQL
