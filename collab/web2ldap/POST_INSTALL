# $Id: POST_INSTALL,v 1.2 2002/06/03 03:42:23 sergeyli Exp $
message "${MESSAGE_COLOR}Map your CGI or FastCGI handler in Apache's `find /etc -name httpd.conf`${DEFAULT_COLOR}"
message "${MESSAGE_COLOR}to `find /usr/share/$SPELL -name web2ldap.py`, or use stand-alone server${DEFAULT_COLOR}"
message "${MESSAGE_COLOR}at `find /usr/share/$SPELL -name standalone.py`.${DEFAULT_COLOR}"
