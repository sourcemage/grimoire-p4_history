requires webserver &&

depends autoconf &&
depends libtool &&
depends bison &&
depends neon "--with-neon=$INSTALL_ROOT/usr" &&
depends db '--with-berkeley-db' &&
optional_depends swig             \
                 '--with-swig'    \
                 '--without-swig' \
                 'for generation of Python bindings with SWIG'
