(

  ./configure  --prefix=${INSTALL_ROOT}/usr/lib/zope  &&
  make                                                &&
  prepare_install                                     &&
  make install

) >$C_FIFO 2>&1
