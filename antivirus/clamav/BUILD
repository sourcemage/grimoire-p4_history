[[ $CROSS_INSTALL == on ]] && OPTS="$OPTS --host=${HOST} --build=${BUILD}"
    default_build
#    default_build                                       &&
#case $CLAMDMAIL in
#y|Y|j|J) cd clamdmail*                                  &&
#  default_build;;
#*) true
#esac
