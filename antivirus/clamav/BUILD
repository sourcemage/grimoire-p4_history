(
  default_build						&&
  cd clamdmail*                                 	&&
  ./configure --prefix=${INSTALL_ROOT}/usr $OPTS	&&
  make                   				&&
  make    install        			
) > $C_FIFO 2>&1
