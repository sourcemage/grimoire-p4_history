# Start scanlogd service
 if  [  -x  /etc/init.d/scanlogd.sh  ];  then
     /etc/init.d/scanlogd.sh  start
 else
     if  [  -x  /etc/init.d/scanlogd  ];  then
         /etc/init.d/scanlogd  start
     fi
 fi
