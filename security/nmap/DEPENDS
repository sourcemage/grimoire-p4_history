depends g++     &&
if [ "$NMAPFE" == "y" ]; then
 if [ "$NMAPFE_GTK" == "gtk+" ]; then
  depends gtk+
 fi                                  &&
 if [ "$NMAPFE_GTK" == "gtk+2" ]; then
  depends gtk+2
 fi
fi
