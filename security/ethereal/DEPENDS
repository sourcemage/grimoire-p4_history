depends  glibc  '--enable-threads'  &&  # *cough*
depends  gtk+  &&
depends  glib  &&
depends  libpcap  &&

optional_depends  'gtk+2'  '--enable-gtk2'  '--disable-gtk2'  \
      'Use gtk+2 inteface instead of gtk+'
