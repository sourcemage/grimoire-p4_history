default_install                             &&

# compatibility (needed for xman)
pushd $INSTALL_ROOT/usr/bin                 &&
ln -s soelim zsoelim                        &&
popd                                        &&
pushd $INSTALL_ROOT/usr/share/man/man1      &&
ln -s soelim.1 zsoelim.1                    &&
popd
