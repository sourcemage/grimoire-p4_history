(

    make all                                    &&
  
    prepare_install                             &&

    mkdir           /usr/share/doc/$SPELL       &&
    cp  thinkCSpy.* /usr/share/doc/$SPELL

) > $C_FIFO 2>&1
