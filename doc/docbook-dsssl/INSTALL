install --directory $INSTALL_ROOT/usr/share/sgml/stylesheets/docbook/dsssl  &&

install -m 644  $SCRIPT_DIRECTORY/docbook-dsssl.cat                         \
                $INSTALL_ROOT/etc/sgml                                      &&

cp -a   common contrib docsrc dtds frames html                              \
        images lib olink print catalog                                      \
        $INSTALL_ROOT/usr/share/sgml/stylesheets/docbook/dsssl
