(

    make                                                &&

    prepare_install                                     &&
    
    cp  abc2mtex search sort_in  /usr/bin               &&
    
    mkdir -p          /usr/doc/$SPELL                   &&
    cp  usrguide.*    /usr/doc/$SPELL                   &&
    cp  *.abc *.txt   /usr/doc/$SPELL

) > $C_FIFO 2>&1
