doesn't build xterm by default, needs a new define
#define BuildXterm YES
in host.def.
This should also be added if it's not there after copying the old host.def, see PRE_BUILD handling of the InstallFontconfigLibrary for how it should be done.
