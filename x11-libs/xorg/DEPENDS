if  [  "$CVS"  ==  "y"  ]; then
  depends CVS
fi  &&

if  [  "$PAM"  ==  "y"  ];  then
  depends  Linux-PAM
fi  &&

depends  bison       &&
depends  flex        &&
depends  perl        &&
depends  fontconfig  &&
depends  zlib        &&
depends  ncurses     &&
depends  expat       &&
#xc/config/cf/linux.cf defines having GhostScript by default
depends  POSTSCRIPT-INTERPRETER  &&

optional_depends  groff                          \
                  ""                             \
                  ""                             \
                  "for formatted documentation"  &&

optional_depends  libpng                   \
                  ""                       \
                  ""                       \
                  "for PNG image support"  &&

optional_depends  libusb                          \
                  ""                              \
                  ""                              \
                  "for USB input device support"
