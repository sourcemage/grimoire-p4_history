if  [  "$CVS"  ==  "y"  ]; then
  depends CVS
fi  &&

if  [  "$PAM"  ==  "y"  ];  then
  depends  Linux-PAM
fi  &&

depends  bison       &&
depends  flex        &&
depends  perl        &&
depends  fontconfig  &&
depends  zlib        &&
depends  ncurses     &&
depends  expat       &&
#xc/config/cf/linux.cf defines this so unless we want to rewrite the spell we need it as full depends
depends  libpng
