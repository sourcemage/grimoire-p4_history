--- python/Makefile.am	2005-03-09 06:23:31.000000000 -0800
+++ python/Makefile.am	2005-03-09 07:05:54.448589352 -0800
@@ -5,7 +5,7 @@
 site_packages = @PYTHONMODDIR@/site-packages
 sip_dir = @datadir@/sip/
 qt_includes = @qt_includes@
-qt_dir = $(dir @qt_includes@)
+qt_dir = ${QTDIR}
 
 python_inc_dir = `echo @PYTHONINC@ | sed s/-I//`
 
@@ -21,7 +21,7 @@
 	@(cd pykde && $(MAKE))
 
 sipdone:
-	cd sip && export QTDIR=${qt_dir} && @PYTHON@ configure.py -b @bindir@ -d ${site_packages} -e ${python_inc_dir} -l qt-mt -v ${sip_dir}
+	cd sip && export QTDIR=${qt_dir} && @PYTHON@ configure.py -b @bindir@ -d ${site_packages} -e ${python_inc_dir} -l qt-mt -v ${sip_dir} INCDIR_QT=@qt_includes@
 	@(cd sip && $(MAKE))
 	echo "" > sipdone
 
