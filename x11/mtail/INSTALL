# Documentation 
DOCDIR=/usr/doc/$SPELL-$VERSION
mkdir -pv $DOCDIR
cp $SOURCE_DIRECTORY/CHANGES $DOCDIR
cp $SOURCE_DIRECTORY/LICENSE $DOCDIR
cp $SOURCE_DIRECTORY/README $DOCDIR
cp $SOURCE_DIRECTORY/mtailrc.sample $DOCDIR

# Install path 
BIN=/usr/bin
cp $SOURCE_DIRECTORY/mtail $BIN
