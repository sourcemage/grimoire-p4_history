depends gtk+2 &&
depends xine-lib &&
depends GConf2 &&
depends gnome-vfs2 &&

optional_depends libiconv \
                 '--with-libiconv-prefix=/usr' \
                 '--without-libiconv-prefix' \
                 'for i18n support'
