default_pre_build                           &&
cp $SCRIPT_DIRECTORY/easytag-patch.diff $SOURCE_DIRECTORY &&
cd $SOURCE_DIRECTORY                        &&
patch -p1 <easytag-patch.diff		    &&
sedit "s/^LIBS =/\0 -lstdc++/" Makefile.in
