  if [ "$CVS" == "y" ] ; then ./autogen.sh; fi   &&

  ./configure  --build=$BUILD                     \
               --prefix=/usr                      \
               --sysconfdir=/etc                  \
               --localstatedir=/var               \
               $OPTS                              &&
  make 
#  cd $SOURCE_DIRECTORY                            &&

