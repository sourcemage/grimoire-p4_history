export PYTHON_LIB=$(/usr/bin/python -c 'from distutils.sysconfig import get_python_lib; print get_python_lib()')

# The following code attempts to precompile source code, but doesn't work
#export PYTHON_PATH=$(pwd) &&
#find -name '*.py' -exec python {} -c "from py_compile import main; main();" ';'
