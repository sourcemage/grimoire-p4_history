(

  prepare_install                                &&
  mkdir  -p     /usr/lib/xemacs/xemacs-packages  &&
  cd            /usr/lib/xemacs/xemacs-packages  &&
  unpack        ${SOURCE}  ${MD5[0]}

) > $C_FIFO 2>&1


