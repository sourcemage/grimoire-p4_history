depends pkgconfig &&
depends perl &&

optional_depends libiconv \
                 '--with-libiconv=gnu' \
                 '--with-libiconv=no' \
                 'for character encoding conversions'
