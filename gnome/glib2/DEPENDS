depends pkgconfig &&
depends perl &&

optional_depends libiconv \
                 '--with-libiconv=gnu' \
                 '--with-libiconv=native' \
                 'for character encoding conversions'
