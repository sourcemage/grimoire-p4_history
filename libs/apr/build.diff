diff -purN apr-0.9.5/Makefile.in apr/Makefile.in
--- apr-0.9.5/Makefile.in	2004-09-01 09:53:50.000000000 +0000
+++ apr/Makefile.in	2005-01-22 23:41:54.000000000 +0000
@@ -87,9 +87,11 @@ install: $(TARGET_LIB) apr-config.out
 	if [ -f shlibtool ]; then \
 		$(LIBTOOL) --mode=install cp shlibtool $(DESTDIR)$(installbuilddir); \
 	fi;
-	if [ -f build/apr_rules.mk ]; then \
-		cp build/apr_rules.mk $(DESTDIR)$(installbuilddir); \
-	fi;
+	for f in apr_common.m4 apr_rules.mk config.guess config.sub find_apr.m4 install.sh make_exports.awk make_var_export.awk mkdir.sh PrintPath; do \
+		if [ -f build/$$f ]; then \
+			cp build/$$f $(DESTDIR)$(installbuilddir); \
+		fi \
+	done;
 
 	if [ ! -d $(DESTDIR)$(bindir) ]; then \
 	    $(top_srcdir)/build/mkdir.sh $(DESTDIR)$(bindir); \
