diff -Nur kaffe-1.0.6.org/configure kaffe-1.0.6/configure
--- kaffe-1.0.6.org/configure	Mon Jul 24 21:47:29 2000
+++ kaffe-1.0.6/configure	Fri Mar 22 03:55:48 2002
@@ -4045,10 +4045,10 @@
 /* Override any gcc2 internal prototype to avoid an error.  */
 /* We use char because int might match the return type of a gcc2
     builtin and then its argument prototype would still apply.  */
-char mpz_get_d();
+char __gmpz_get_d();
 
 int main() {
-mpz_get_d()
+__gmpz_get_d()
 ; return 0; }
 EOF
 if { (eval echo configure:4055: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; } && test -s conftest${ac_exeext}; then
