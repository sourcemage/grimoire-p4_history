if ! grep -q CONFIGURED ${SPELL_CONFIG}
 then 
 if query "Would you like to include the examples" y
  then echo 'examples="y"' > ${SPELL_CONFIG}
  else echo 'examples="n"' > ${SPELL_CONFIG}
 fi
 echo  'CONFIGURED="y"'  >>  ${SPELL_CONFIG}
fi
