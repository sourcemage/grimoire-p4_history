default_pre_build &&
#if [ "$LIBEXIF_CVS" == "y" ]; then
  cd "$SOURCE_DIRECTORY" &&
  sedit "s/doc b/b/" Makefile.am &&
  NOCONFIGURE=ON ./autogen.sh &&
#else
#fi  &&
patch -p0 <$SCRIPT_DIRECTORY/exif-utils.h.patch  
