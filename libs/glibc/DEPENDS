depends  autoconf  &&
depends  gettext   &&
depends  perl      &&
depends   "glibc-kernel-headers" "" "" "due to broken 2.6 series headers, this simplifies the sanitisation 
process, but for now it actually defaults to 2.4 headers :)"
