if  [  "$UTF8"  ==  "y"  ]
then
  OPTS="$OPTS  --enable-widec"
fi  &&
OPTS="$OPTS --with-shared --libdir=${INSTALL_ROOT}/lib"  &&
make_single                                              &&
default_build
