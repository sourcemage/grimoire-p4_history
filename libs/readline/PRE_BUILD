default_pre_build
cd      $SOURCE_DIRECTORY

for i in 2 3 5 6
do
        SVAR=SOURCE$i
        message "${MESSAGE_COLOR} Applying patch ${!SVAR} ${DEFAULT_COLOR}"
        unpack ${!SVAR} ${MD5[$(($i-1))]}
        patch -p2 < $SOURCE_CACHE/${!SVAR}
done
