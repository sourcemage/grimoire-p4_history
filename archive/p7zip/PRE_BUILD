default_pre_build &&
cd $SOURCE_DIRECTORY &&

sedit "s|/usr/local|$INSTALL_ROOT/usr|g" makefile &&

# TODO copy appropriate file
case $(uname -m) in
    i*86) cp makefile.linux_x86 makefile.machine
          ;;
#    ppc)
# sparc*)
       *) message "${ERROR_COLOR}Unsupported architecture${DEFAULT_COLOR}" &&
          false
          ;;
esac
