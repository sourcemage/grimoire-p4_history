#!/bin/bash

RUNLEVEL=3
NEEDS="+network +remote_fs"

. /etc/init.d/smgl_init

case $1 in
  start)    echo "Starting jabberd..."
            daemon -n jabberd jabberd
            evaluate_retval
            ;;

  stop)     echo "Stopping jabberd..."
            daemon -n jabberd --stop
            evaluate_retval
            ;;

  restart)  echo "Restarting jabberd..."
            daemon -n jabberd --restart
            evaluate_retval
            ;;

  status)   daemon -v -n jabberd --running
            ;;

  *)        echo "Usage: $0 {start|stop|restart|status}"
            exit 1
            ;;
esac
