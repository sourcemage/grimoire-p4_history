( 
 prepare_install                        &&
 INSTALL_DIR=/usr/share/tkabber         && 
 mkdir $INSTALL_DIR                     && 
 cp -a $SOURCE_DIRECTORY/* $INSTALL_DIR && 
 cp $SCRIPT_DIRECTORY/tkabber /usr/bin 
) > $C_FIFO 2>&1
