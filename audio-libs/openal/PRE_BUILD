default_pre_build &&
cd ${SOURCE_DIRECTORY} &&
#patch -p0 < $SCRIPT_DIRECTORY/openal-build-patch0.diff  &&
#patch -p0 < $SCRIPT_DIRECTORY/openal-no-march.patch     &&

rm -rf `find -type d -name CVS`                         &&

cd linux                                                &&
NOCONFIGURE=Y ./autogen.sh
