default_pre_build &&
cd ${SOURCE_DIRECTORY} &&
patch -p1 < $SCRIPT_DIRECTORY/openal.cummulative.patch  &&

rm -rf `find -type d -name CVS`                         &&

cd linux                                                &&
NOCONFIGURE=Y ./autogen.sh
