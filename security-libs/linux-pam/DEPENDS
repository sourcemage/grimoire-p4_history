depends autoconf                               &&
if [ "${PAM_SELINUX}" == "y" ]; then
  depends libselinux &&
  depends gcc34
fi                                             &&

depends cracklib                               &&

if [ "${PAM_DOCS}" == "y" ]; then
  depends tetex                                &&
  depends linuxdoc-tools                       
fi 
