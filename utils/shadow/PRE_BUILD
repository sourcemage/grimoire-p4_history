default_pre_build                               &&
cd $SOURCE_DIRECTORY                            &&
if [ "$SELINUX" == "y" ]; then
  unpack  $SOURCE_CACHE/$SOURCE2  ${MD5[1]}     &&
  zcat    $SOURCE_CACHE/$SOURCE2  |  patch  -p1 &&
  autoconf
fi
