if  [  !  -e  ${INSTALL_ROOT}/etc/sysconfig/hwclock  ];  then
  if  query  "Should the hardware clock store time in UTC (may break systems like MS Windows)?"  y;  then
    UTC=yes
  else
    UTC=no
  fi
  export UTC
fi
config_query  SELINUX   "Do you want to enable selinux support?"  n
