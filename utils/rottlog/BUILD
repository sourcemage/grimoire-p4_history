(
  default_build                                    &&
  cp $SCRIPT_DIRECTORY/sample* /etc/rottlog	   &&
  cp $SCRIPT_DIRECTORY/syslogd /etc/rottlog	   &&
  mkdir -p /usr/share/doc/rottlog                  &&
  cp $SCRIPT_DIRECTORY/README.SOURCEMAGE /usr/share/doc/rottlog

) > $C_FIFO 2>&1
