service sgi_fam
{
        type         =  RPC UNLISTED
        socket_type  =  stream
        user         =  root
        group        =  nogroup
        server       =  /usr/bin/fam
        wait         =  yes
        protocol     =  tcp
        rpc_version  =  2
        rpc_number   =  391002
}
