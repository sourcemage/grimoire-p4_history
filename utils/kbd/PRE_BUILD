default_pre_build                                          &&
patch  -p1  data/Makefile  $SCRIPT_DIRECTORY/keymaps.diff  &&

# Fix for gcc4
if use_gcc ^4; then
  cd $SOURCE_DIRECTORY                                     &&
  patch -p0 < $SCRIPT_DIRECTORY/gcc4_fix.patch
fi
