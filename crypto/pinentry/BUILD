(

  case $KMail in

    y|Y) OPTS="$OPTS --disable-pinentry-gtk      \
                     --disable-pinentry-curses   \
                     --disable-fallback-curses"
         ;;
  esac

  default_build 

) > $C_FIFO 2>&1
