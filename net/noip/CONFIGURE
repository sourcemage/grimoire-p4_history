if ! grep -q "NOIP_BOOT" $SPELL_CONFIG; then
  if    query  "Invoke noip at boot?" y
  then  
        echo   "NOIP_BOOT=y"  >  $SPELL_CONFIG
  else  
        echo   "NOIP_BOOT=n"  >  $SPELL_CONFIG
  fi
fi
