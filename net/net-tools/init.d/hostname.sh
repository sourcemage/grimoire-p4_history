#!/bin/sh
# SGL-script-version=20021024
# set the above to custom instead of date format if you use
# a custom hostname.sh script
# this sets the run levels and priority for links
# SGL-START:S:S20

/bin/hostname  -F  /etc/hostname
