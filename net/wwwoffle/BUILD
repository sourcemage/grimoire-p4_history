(

  ./configure --prefix=/usr --sysconfdir=/etc  --localstatedir=/var --with-spooldir=/var/spool/wwwoffle &&

  make							&&
  prepare_install                                	&&
  make    install

) > $C_FIFO 2>&1  
