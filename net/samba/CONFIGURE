if ! grep -q "CONFIGURED"  ${SPELL_CONFIG}
then

  if    query  "Enable Active Directory Support ?\n(Requires OpenLDAP and krb5)"  y
  then  AD="y"
  fi

  echo  "AD=${AD}"        >>  ${SPELL_CONFIG}
  echo  "CONFIGURED='y'"  >>  ${SPELL_CONFIG}

fi
