message "${MESSAGE_COLOR}" 								&&
message "Please note that since udev 0.51, udev.permissions file is no longer used." 	&&
message "udev.rules file syntax has changed to include the permissions stuff. If you"	&&
message "don't update your /etc/udev/udev.rules, your system might be broken. Please"	&&
message "do it before rebooting, or don't update the spell ${DEFAULT_COLOR}"       	&&

config_query  UDEV_UPDATE  "Do you still wish to continue?"  n  			&&

if  [  "$UDEV_UPDATE"  ==  "n"  ];  then
  return  1
else
  message "A typical udev.rules will be placed in /etc/udev/udev.rules.example"
fi
