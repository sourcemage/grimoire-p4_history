Spell needs the following tests:
- does it compile with 2.4 headers?
- does the generated binary work for burning cds? (don't have a spare CD-R(W) right now) - checked, it does work (jbernardo@sourcemage.org)
