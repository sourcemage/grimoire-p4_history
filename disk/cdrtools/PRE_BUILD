default_pre_build      &&
cd  $SOURCE_DIRECTORY  &&
unpack  $SOURCE2       &&

(  patch  -p1  <  zisofs-tools-1.0.3/cdrtools-1.11a09-zisofs.diff
   true
   # H. Peter Avin, author of zisofs-tools says
   # "I just looked, and the rejected hunks are completely harmless, so
   # you can just apply the patch anyway and it should work."
)
