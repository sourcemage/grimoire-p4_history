depends openssl 			&&
optional_depends  tcp_wrappers          \
                  "--with-tcp-wrappers" \
                  ""                    \
                  " to allow netatalk to check hosts.allow/deny"
