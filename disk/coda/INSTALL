if  [  "$CODA_SVR"  =  "y"  ];  then
  message  "${MESSAGE_COLOR}Building coda server${DEFAULT_COLOR}"
  make  server-install
fi &&

message  "${MESSAGE_COLOR}Building coda client${DEFAULT_COLOR}"  &&
make  client-install
