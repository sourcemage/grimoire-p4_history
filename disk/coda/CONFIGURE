if  !  grep  -q  CONFIGURED  $SPELL_CONFIG;  then

  if  query  "Do you want to install the coda server?" n
    then  echo  'CODA_SVR="y"'  >>  $SPELL_CONFIG
  fi

  echo  'CONFIGURED="y"'  >>  $SPELL_CONFIG
fi
