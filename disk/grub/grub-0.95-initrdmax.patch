diff -urpN grub-0.95.orig/stage2/boot.c grub-0.95/stage2/boot.c
--- grub-0.95.orig/stage2/boot.c	2004-11-09 13:25:38.191277304 -0800
+++ grub-0.95/stage2/boot.c	2004-11-09 13:55:49.518913648 -0800
@@ -824,8 +824,12 @@ load_initrd (char *initrd)
     moveto = (mbi.mem_upper + 0x400) << 10;
   
   moveto = (moveto - len) & 0xfffff000;
+#if 0
   max_addr = (lh->header == LINUX_MAGIC_SIGNATURE && lh->version >= 0x0203
 	      ? lh->initrd_addr_max : LINUX_INITRD_MAX_ADDRESS);
+#else
+  max_addr = LINUX_INITRD_MAX_ADDRESS;
+#endif
   if (moveto + len >= max_addr)
     moveto = (max_addr - len) & 0xfffff000;
   
