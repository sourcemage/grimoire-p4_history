default_pre_build                                                        &&
cd  $SOURCE_DIRECTORY                                                    &&

if  [  "$GRUB_REISER4"  =  "y"  ];  then
	patch  -p1  <  ${SCRIPT_DIRECTORY}/grub-0.96-reiser4-20040130.patch
else
	patch  -p1  <  ${SCRIPT_DIRECTORY}/grub-0.96-graphics.patch
fi
