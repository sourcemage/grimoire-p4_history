--- setup.cfg.cp	2003-11-29 18:51:06.000000000 +0100
+++ setup.cfg	2003-11-29 18:54:35.000000000 +0100
@@ -14,19 +14,14 @@
 # On Windows, setup.py will try to determine whether the database is
 # Firebird 1.0 or later by reading the registry.  To force the database into
 # Firebird mode, uncomment this:
-#database_is_firebird=1
+database_is_firebird=1
 
 # Installation directory of the database.  Unless otherwise specified, include
 # and lib paths are relative to the database_home_dir.
-#database_home_dir=c:/dev/db/firebird/core
+database_home_dir=/opt/firebird
 
-#database_include_dir=c:/dev/db/firebird/core/include
-# For IB 5.5:
-#database_include_dir=c:/dev/db/ib55/include
-
-#database_lib_dir=c:/dev/db/firebird/core/lib
-# For IB 5.5:
-#database_lib_dir=c:/dev/db/ib55/lib
+database_include_dir=/usr/include/firebird
+#database_lib_dir=/usr/lib
 
 # Firebird 1.5 will work with the gds library, but its native lib is:
 # (on Windows+MSVC|MinGW):
@@ -36,6 +31,8 @@
 # (on Linux+GCC with Classic architecture):
 #database_lib_name=fbembed
 
+#database_lib_name=fbclient
+
 ##############################################################################
 # Generic distutils options:
 ##############################################################################
