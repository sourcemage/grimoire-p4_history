. ${SPELL_CONFIG}

# show that installation works
  VV=`gpre -z 2>/dev/null| grep Fire |cut -c 14-`&&
  echo $VV installed
  pkill fbserver		
# just in case it's running, since gpre may have started it,
# which would prevent the init.d server starting

cat << EOF
remember to create a "firebird.conf" file
see the examples directory for more information

Also change the default passwords 

Please note that program "isql" has been renamed as "fbisql"

EOF
echo $REMIND

if [ "$INST" == "classic" ] ;then cat <<EOF
you should install the xinetd/firebird script manually
and restart xinetd after installation
EOF
fi

		
