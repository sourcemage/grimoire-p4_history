mk_source_dir  $SOURCE_DIRECTORY                        &&
unpack_file                                             &&
cd	       $SOURCE_DIRECTORY                            &&
unpack_hash         ${SOURCE3} ${SOURCE3_HASH}          && 

#patch the file
patch -p1 < $SPELL_DIRECTORY/config.patch               &&
cd ..
