case "${BRANCH}" in
  stable)
    patch -p1 < "${PATCH_DIRECTORY}/${PATCH}/patch-1.4.0.cd.trash_folder.3.1"
  ;;
  devel|cvs)
    patch -p1 < "${PATCH_DIRECTORY}/${PATCH}/patch-1.5.5.1.cd.trash_folder.3.4"
  ;;
esac
