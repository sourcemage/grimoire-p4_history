case "${BRANCH}" in
  stable)
    PATCH_SOURCE="patch-1.4.0.cd.trash_folder.3.1"
  ;;
  devel|cvs)
    PATCH_SOURCE="patch-1.5.5.1.cd.trash_folder.3.4"
  ;;
esac &&

cp "${PATCH_DIRECTORY}/${PATCH}/${PATCH_SOURCE}" "${PATCH_SOURCE}" &&
patch -p1 < "${PATCH_SOURCE}"
