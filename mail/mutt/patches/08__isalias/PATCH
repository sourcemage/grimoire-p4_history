case "${BRANCH}" in
  stable)
    cp "${PATCH_DIRECTORY}/${PATCH}/patch-1.4.dgc.isalias.1" patch-1.4.dgc.isalias.1 &&
    patch -p0 < "${PATCH_DIRECTORY}/${PATCH}/patch-1.4.dgc.isalias.1.smgl.diff" &&
    patch -p1 < patch-1.4.dgc.isalias.1
  ;;
  devel|cvs)
    patch -p1 < "${PATCH_DIRECTORY}/${PATCH}/patch-1.5.8.dgc.isalias.2"
  ;;
esac
