case "${BRANCH}" in
  stable)
    patch -p1 < "${PATCH_DIRECTORY}/${PATCH}/patch-1.4.dgc.isalias.1.smgl"
  ;;
  devel|cvs)
    patch -p1 < "${PATCH_DIRECTORY}/${PATCH}/patch-1.5.8.dgc.isalias.2"
  ;;
esac
