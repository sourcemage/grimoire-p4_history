depends perl		&&
depends DBI		&&
depends Digest		&&
depends Digest-MD5	&&
depends HTML-Tagset	&&
depends DBD-SQLite

# the name of relevant DBI module is contained in 
# /usr/share/popfile/Classifier/Bayes.pm 
# which will need ot be modified to use something other than SQLite

#optional_depends DBD-SQLite    "" "" "new style popfile database(small)"&&
#optional_depends DBD-InterBase "" "" "new style popfile database(large)"


# BerkeleyDB is only for existing popfile installations, which already 
# have it installed, so we don't need a depends for it
#optional_depends BerkeleyDB    "" "" "if upgrading a popfile-0.20.x database"

