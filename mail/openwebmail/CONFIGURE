if [ ! -x /usr/bin/suidperl ]; then
 message "You have to configure perl with setuid/setgid emulation enabled."
 message "Do a cast -r perl and answer y to this option..."
 message "This is MANDATORY."
fi

message "Remember to edit /etc/openwebmail.conf to match your needs."
