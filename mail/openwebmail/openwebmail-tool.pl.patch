--- openwebmail-tool.pl.org	2003-02-11 22:04:37.000000000 +0100
+++ openwebmail-tool.pl	2003-02-11 22:13:04.000000000 +0100
@@ -368,19 +368,19 @@
          qq|$content\n|.
          qq|Send the site report?(Y/n) |;
 
-   if ($opt{'yes'}) {
-      print qq|Yes.\n|;
-      print qq|sending report...\n|;
-      send_mail("$id\@$hostname", $realname, $to, $date, $subject, "$content \n");
-   } elsif ($opt{'no'}) {
+#   if ($opt{'yes'}) {
+#      print qq|Yes.\n|;
+#      print qq|sending report...\n|;
+#      send_mail("$id\@$hostname", $realname, $to, $date, $subject, "$content \n");
+#   } elsif ($opt{'no'}) {
       print qq|No.\n|;
-   } else {
-      $_=<STDIN>;
-      if ($_!~/^n/) {
-         print qq|sending report...\n|;
-         send_mail("$id\@$hostname", $realname, $to, $date, $subject, "$content \n") 
-      }
-   }
+#   } else {
+#      $_=<STDIN>;
+#      if ($_!~/^n/) {
+#         print qq|sending report...\n|;
+#         send_mail("$id\@$hostname", $realname, $to, $date, $subject, "$content \n") 
+#      }
+#   }
    print qq|\nThank you.\n\n|;
    return 0;
 }
@@ -490,9 +490,10 @@
    }
 
    if ($errmsg) {
-      print $errmsg;
-      print qq|\nAnd execute '$SCRIPT_DIR/openwebmail-tool.pl --init' again!.\n\n|;
-      return -1;
+#      print $errmsg;
+#      print qq|\nAnd execute '$SCRIPT_DIR/openwebmail-tool.pl --init' again!.\n\n|;
+#      return -1;
+      return 0;
    } else {
       return 0;
    }
