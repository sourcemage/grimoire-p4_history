if  [  -x  ${INSTALL_ROOT}/etc/init.d/courierinit.sh  ];  then
  ${INSTALL_ROOT}/etc/init.d/courierinit.sh  start
fi

if  [  -x  ${INSTALL_ROOT}/etc/init.d/esmtpd.sh       ];  then
  ${INSTALL_ROOT}/etc/init.d/esmtpd.sh       start
fi

if  [  -x  ${INSTALL_ROOT}/etc/init.d/esmtpd-ssl.sh   ];  then
  ${INSTALL_ROOT}/etc/init.d/esmtpd-ssl.sh   start
fi

if  [  -x  ${INSTALL_ROOT}/etc/init.d/filterctl.sh    ];  then
  ${INSTALL_ROOT}/etc/init.d/filterctl.sh    start
fi

if  [  -x  ${INSTALL_ROOT}/etc/init.d/imapd.sh        ];  then
  ${INSTALL_ROOT}/etc/init.d/imapd.sh        start
fi

if  [  -x  ${INSTALL_ROOT}/etc/init.d/imapd-ssl.sh    ];  then
  ${INSTALL_ROOT}/etc/init.d/imapd-ssl.sh    start
fi

if  [  -x  ${INSTALL_ROOT}/etc/init.d/pop3d.sh        ];  then
  ${INSTALL_ROOT}/etc/init.d/pop3d.sh        start
fi

if  [  -x  ${INSTALL_ROOT}/etc/init.d/pop3d-ssl.sh    ];  then
  ${INSTALL_ROOT}/etc/init.d/pop3d-ssl.sh    start
fi
