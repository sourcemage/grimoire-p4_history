if  spell_installed  "apache";  then
  LOC="/usr/share/httpd/htdocs/squirrelmail/plugins/"
elif  spell_installed  "apache2";  then
  LOC="/usr/share/apache2/htdocs/squirrelmail/plugins/"
fi

# The following is dirty, but the only way I can think of
# to get the use of $NAME, as it doesn't stay past the
# build part.
source  $SCRIPT_DIRECTORY/DETAILS 1>/dev/null 2>&1
rm  -rf  $LOC/$NAME
