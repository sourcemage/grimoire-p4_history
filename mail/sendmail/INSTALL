make install-cf                    &&
cd ../..                           &&
make install                       &&

cp -r cf        /usr/doc/sendmail  &&
cp -r devtools  /usr/doc/sendmail  &&
cp -r contrib   /usr/doc/sendmail
