default_pre_build                                  &&
cd  $SOURCE_DIRECTORY                              &&

if  [ "$BUILD_ENIGMAIL" = "y" ];  then
  cd  $SOURCE_DIRECTORY/extensions  &&
  unpack_file  3  &&
  unpack_file  5
fi
