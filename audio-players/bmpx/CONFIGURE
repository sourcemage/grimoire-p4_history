# Disabled untl gstreamer 0.9x is in grimoire
#
#if    spell_ok  gstreamer;  then
#  config_query_list  BMPX_BACKEND  'Use which backend?'  gstreamer xine-lib
#elif  spell_ok  xine-lib;  then
#  config_query_list  BMPX_BACKEND  'Use which backend?'  xine-lib gstreamer
#fi

#config_query ONEPLUGINDIR "Enable one plugin directory?" n                        &&
#config_query USERPLUGINDIR "Enable user plugin directory?" y                      &&
#config_query SIMD "Enable 3DNow-MMX? Do *NOT* use on x86_64 or ppc platforms " n  &&
#config_query OSS "Enable OSS output plugin" y                                     &&
#config_query SKINS "Download some skins?" y
